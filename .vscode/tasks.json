{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "nixos-rebuild switch",
      "type": "shell",
      "command": "sudo cp ${workspaceFolder}/prometheus/*.nix /etc/nixos/ && sudo cp ${workspaceFolder}/prometheus/flake.lock /etc/nixos/ && sudo cp -r ${workspaceFolder}/config /etc/nixos/ && sudo chown -R root:root /etc/nixos/ && sudo nixos-rebuild switch --flake .#prometheus && echo 😁",
      "icon": {
        "color": "terminal.ansiBlue",
        "id": "symbol-property"
      },
      "presentation": {
        "echo": true,
        "reveal": "always",
        "focus": false,
        "panel": "shared",
        "showReuseMessage": false,
        "clear": true,
        "close": false
      },
      "problemMatcher": []
    }
  ]
}